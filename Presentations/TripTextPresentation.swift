/** Present a `Trip` in a way that's consumable in a CLI environment. */
class TripTextPresentation
{
    typealias Presented = Trip
    var parent: MarketTextPresentation?
    
    /** Sorted list of `Presentation`s for all trip's items */
    
    /** Sorted list of `Presentation`s for non-crossed-off items */
    /** Sorted list of `Presentation`s for crossed-off items */
}

/** Adding or deleting a purchase */

